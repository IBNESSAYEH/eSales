server:
  port: 8080
spring:
  cloud:
    gateway:
      routes:
        - id: product-service
          uri: lb://product-service
          predicates:
            - Path=/product/**
      discovery:
        locator:
          enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka


